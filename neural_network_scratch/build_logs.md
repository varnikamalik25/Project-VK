# Neuron
- single neuron is just a function modeling weights, bias and inputs, 
- for first experiments: the function looks like this,
- `sum = Summation, n = 0 to M inputs[n] * weights[n]`
- `output = sum + bias` 
- This is How the neuron works.

## Multiple Neurons Handled Via OOPS:
- I added OOPs because I didn't like write the exact same code three times.

- replacement with numpy for faster processing.